// storage.js - Handles data storage and retrieval (e.g., localStorage, database). Person 3: Implement storage logic here. 
 
function saveLoanData(loan) { 
  // TODO: Save loan object to storage (e.g., localStorage.setItem('loans', JSON.stringify(loan))); 
  console.log('Loan data saved:', loan); 
} 
 
function getLoanData(id) { 
  // TODO: Retrieve loan by ID from storage; 
  return {}; // Placeholder 
} 
 
module.exports = { saveLoanData, getLoanData }; 
